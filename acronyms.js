// Techies love their TLA (Three Letter Acronyms)!

// Help generate some jargonby writing a program that converts a long name like Portable Network Graphics 
// to its acronym (PNG).

function acroCreator(a,b,c)
{
	console.log(a[0] + b[0] + c[0])
}

acroCreator("Hello" , "World" , "Derp")

console.log("-------------------------------------------------")
